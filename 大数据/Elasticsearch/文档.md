#### 通过查询进行更新

```json
POST twitter/_update_by_query
{
  "script": {
    "inline": "ctx._source.count++",
    "lang": "painless"
  },
  "query": {
    "term": {
      "user": "kimchy"
    }
  }
}
```

